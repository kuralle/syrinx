# Final WBS — Gemini Validation Follow-Up

Source: `GEMINI-VALIDATION.md` validated against production-code `HEAD` `d5448ff`; the final docs-only commit does not modify code.

Scope rule: fix only the **CONFIRMED-REAL** defects and the actionable real portions of **PARTIALLY-REAL** rows. Exclude already-fixed, false-positive, and unproven design-theory portions.

## Recommended Fix Order

| Order | Work item | Validation IDs | Severity | Effort | Files | Approach | Dependencies |
|---:|---|---|---|---|---|---|---|
| P0-1 | Fix latency-filler word-boundary text mangling. | W6-VE03-2 | high | S | `packages/voice/src/latency-filler.ts`, `packages/voice/src/latency-filler.test.ts` | Make redundant-prefix stripping require punctuation/whitespace/end boundary after the filler word; add cases for `Some`, `Social`, `Wellness`, `Welcome`, and real duplicate filler text. | None |
| P0-2 | Fix browser jitter-buffer barge-in gap. | W2-WT03-2, W2-WT03-3, W2-WT03-4 | high | M | `packages/voice-client-browser/src/audio.ts`, `packages/voice-client-browser/src/jitter-buffer.test.ts`, `packages/voice-client-browser/src/index.ts` | On context clear, recompute `nextScheduledTime` from remaining frames or reset when empty; assert next-frame schedule/buffer duration; clear buffered playout on unexpected close before reconnect. | None |
| P0-3 | Repair primary-speaker gate suppression correctness. | W6-VE02-4, W6-VE02-5, W6-VE02-6 | high | M | `packages/voice/src/primary-speaker-gate.ts`, `packages/voice/src/turn-arbiter.ts`, `packages/voice/src/voice-agent-session.ts`, focused tests | Gate enrollment on active VAD speech rather than all pre-TTS audio; change echo suppression to require echo dominance over primary by margin; keep/re-evaluate pending state after non-primary suppression instead of ending the whole VAD turn. | Add characterization tests first; do before any speaker-fingerprint tuning. |
| P0-4 | Add browser downlink codec capability/fallback handshake. | W4-WT07-1 | high | M | `packages/voice-client-browser/src/index.ts`, `packages/voice-server-websocket/src/index.ts`, protocol tests/smokes | Add an explicit client codec-capability or negotiated-codec message after Opus load result; server chooses PCM downlink when client cannot decode Opus; ensure decode errors stay non-fatal but do not leave playout broken. | Define protocol shape before implementation. |
| P1-1 | Fix `SessionStore` correctness for resume and shutdown. | W3-WT06-1, W3-WT06-2, W3-WT06-3, W5-WT09-2 | high | L | `packages/voice-server-websocket/src/session-store.ts`, `packages/voice-server-websocket/src/index.ts`, `packages/voice-server-websocket/src/turn-metrics.ts` | Serialize per-session lease creation; let `release(id, 0)` cancel timers and evict immediately; add a store update seam or encapsulated mutable session-state API; persist in-flight turn metric state across resumed sockets. | Should precede broader distributed-store implementations. |
| P1-2 | Make streaming resampling stateful and test chunk boundaries. | W0-WT02-1, W0-WT02-2 | high | L | `packages/voice/src/audio/resample.ts`, `packages/voice/src/audio/audio.test.ts`, callers that resample streams | Introduce a stateful streaming resampler/decimator with overlap/history and update hot-path callers to reuse it per stream; keep stateless helper only for whole-buffer transforms if still needed; add 20 ms chunk continuity tests. | Audit all resample callers before changing API shape. |
| P1-3 | Fix provider WebSocket dispose/reconnect hangs. | W1-G27-1, W1-G27-2 | high | M | `packages/voice-ws/src/index.ts`, `packages/voice-ws/src/node.ts`, tests | Make socket disposal settle the current open attempt; avoid removing attempt-owned listeners before promise settlement or add an explicit abort signal; clear verification timeout/listeners on dispose. | None |
| P1-4 | Fix graceful browser/server drain semantics. | W2-WT04-1, W2-WT04-2 | high | M | `packages/voice-server-websocket/src/outbound-playout-pipeline.ts`, `packages/voice-server-websocket/src/index.ts`, drain tests | Settle `drainAndClose()` on socket close; after sending 1001, wait for close or a short fallback deadline before `terminate()`; avoid immediate guillotine after a successful graceful close request. | None |
| P1-5 | Fix Smart Turn semantic endpoint latency races. | W5-VE01-2, W5-VE01-3 | high | M | `packages/voice-turn-pipecat/src/index.ts`, `packages/voice-turn-pipecat/src/*.test.ts` | Preserve shortcut intent when VAD-end analysis happens before final STT; when semantic defer expires incomplete, request/finalize immediately instead of stacking another fallback delay. | Add timeline tests for VAD-end before STT-final. |
| P2-1 | Fix pacer control-frame timing. | W2-WT03-1 | med | S | `packages/voice-server-websocket/src/paced-playout.ts`, pacer tests | Advance `nextDeadlineMs` by the frame's actual `durationMs`; ensure 0 ms controls do not insert a 20 ms gap or false deadline miss. | None |
| P2-2 | Fix browser playout metrics accuracy and jitter-smoke coverage. | W5-WT09-1, W5-WT09-3, W5-WT09-4 | med | M | `packages/voice-server-websocket/src/playout-progress.ts`, `packages/voice-server-websocket/src/turn-metrics.ts`, `examples/02-hello-voice-headless/scripts/run-browser-jitter-smoke.ts`, `packages/voice-client-browser/index.html` | Emit/record an unthrottled playout-start signal while keeping throttled progress; impair downlink in the jitter proxy; initialize lead metric as unset/null. | Coordinate with P1-1 for persisted metrics state. |
| P2-3 | Fix Cartesia offset cleanup and precision drift. | W1-VE04-1, W1-VE04-2 | med | M | `packages/voice-tts-cartesia/src/index.ts`, Cartesia tests | Delete `contextAudioOffsetMs` on provider error frames; track cumulative audio samples per context and derive timestamp offsets from sample count. | None |
| P2-4 | Stop unmatched upgrade-path hijacking. | W4-WT08-2 | med | M | `packages/voice-server-websocket/src/websocket-upgrade.ts`, admission/routing tests | Replace unconditional destroy with cooperative routing: either preserve pre-existing upgrade listeners, expose a `notFound` policy, or only handle registered paths without consuming unrelated upgrades. | Needs API decision for shared-server coexistence behavior. |
| P2-5 | Fix browser reconnect edge during `CLOSING`. | W0-WT05-1 | med | S | `packages/voice-client-browser/src/websocket-transport.ts`, `packages/voice-client-browser/src/index.ts`, reconnect tests | Treat `CLOSING` as not connectable but pending: queue requested connect until close, or force-discard the closing socket and open a new one consistently with `cleanClose` semantics. | None |
| P2-6 | Guard transport-host startup against already-closed sockets. | W1-WT01-1 | med | S | `packages/voice-server-websocket/src/transport-host.ts`, startup race test | Initialize `socketClosed` from `readyState`, and abort session acquisition if the socket is already closing/closed before startup. | None |
| P2-7 | Clean latency-filler state on all terminal paths. | W6-VE03-3 | med | S | `packages/voice/src/latency-filler.ts`, `packages/voice/src/voice-agent-session.ts`, tests | Clear filler state on recoverable/fatal turn errors and cancellation terminalization, not only `llm.done` flush. | Can pair with P0-1. |
| P3-1 | Harden test HTTP server cleanup. | W3-WT10-1 | low | S | `packages/voice-server-websocket/src/test-helpers.ts` | Use `closeAllConnections()`/`closeIdleConnections()` when available and/or a bounded fallback so `afterEach` cannot hang forever on untracked keep-alive sockets. | None |
| P3-2 | Remove VE-01 redundant semantic condition. | W5-VE01-1 | low | S | `packages/voice-turn-pipecat/src/semantic-completeness.ts` | Delete the unreachable duplicate `TRAILING_INCOMPLETE` check and keep behavior unchanged. | Can pair with P1-5. |
| P3-3 | Prevent early uplink codec hybrid mode if desired. | W4-WT07-2 | low | S | `packages/voice-client-browser/src/index.ts` | Queue or reject outbound audio until the first `ready`/codec negotiation decision is known; if PCM-first startup is acceptable, document it as intentional. | Best paired with P0-4 protocol work. |

## No Action

| ID | Reason |
|---|---|
| W4-WT08-1 | Already fixed by `66af756 CR-03`; current code exposes `maxConcurrentSessionsScope` and has a cross-path server-scope test. |
| W1-VE04-3 | False positive for actual provider path: Cartesia messages are parsed from JSON, which cannot carry `NaN`; parse failures already clear active contexts and offsets. |
| W6-VE02-1 | The static 6-band fingerprint is a real design limitation, but no concrete production defect was proven. Revisit after P0-3 correctness fixes with real speech/noise fixtures. |
| W6-VE02-2 | RMS volume sensitivity mechanism is real but impact is unquantified. Revisit with fixture-based false-reject/false-accept tests after P0-3. |
| W6-VE02-3 | Positive-vector cosine bias is mathematically plausible but unmeasured. Revisit as part of future speaker-gate model evaluation, not as an immediate bug fix. |
| W6-VE02-7 | Assistant profile lacks an explicit clear, but current barge-in evaluation requires active TTS; no silent-assistant suppression path was confirmed. Consider cleanup only if P0-3 tests reveal stale-profile failures. |
| W6-VE03-1 | Separate filler/main TTS packets are structurally true, but audible gap/prosody severity is provider-dependent and unmeasured. Do not redesign filler splice until the concrete P0/P2 filler bugs are fixed. |
