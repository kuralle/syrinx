# Gemini External Review Validation

Validated against production-code `HEAD` `d5448ff` on 2026-06-01; the final docs-only commit does not modify code. Verdicts are against the current code, not the wave SHAs. `GEMINI-EXTERNAL-REVIEW.md` was read only.

Verdict counts: **CONFIRMED-REAL 32**, **PARTIALLY-REAL 6**, **FALSE-POSITIVE 1**, **ALREADY-FIXED 1**.

## Wave 0

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W0-WT02-1 | Stateless chunk resampling creates edge ringing/tapering. | CONFIRMED-REAL | `packages/voice/src/audio/resample.ts:47-53` explicitly accepts stateless edge tapering; `firDecimate` drops missing context at `packages/voice/src/audio/resample.ts:63-67`. Repro: `pnpm exec tsx -e "import { resamplePcm16 } from './packages/voice/src/audio/resample.ts'; const input=new Int16Array(320).fill(10000); const out=resamplePcm16(input,16000,8000); console.log({min:Math.min(...out),max:Math.max(...out),first:[...out.slice(0,8)]})"` produced `min:7250`, `max:10883` for constant input. | high | high |
| W0-WT02-2 | Anti-alias spectral test misses streaming chunk-boundary distortion. | CONFIRMED-REAL | The test uses one 100 ms block (`N_IN = 1600`) in `packages/voice/src/audio/audio.test.ts:230-260`; it does not resample repeated 20 ms chunks or assert boundary continuity. | med | high |
| W0-WT05-1 | Calling `connect()` while the underlying browser socket is `CLOSING` is silently swallowed. | CONFIRMED-REAL | `SyrinxBrowserClient.connect()` only checks `transport.connected` (`OPEN`) at `packages/voice-client-browser/src/index.ts:215-220`, but `WebSocketClientTransport.connect()` returns whenever `readyState !== CLOSED`, including `CLOSING`, at `packages/voice-client-browser/src/websocket-transport.ts:23-26`. `close()` sets `cleanClose = true` at `packages/voice-client-browser/src/index.ts:223-228`, so the later close event will not reconnect. | med | high |

## Wave 1

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W1-WT01-1 | `runWebSocketConnection` starts with `socketClosed = false` and can miss an already-closed socket. | CONFIRMED-REAL | Current code still initializes `socketClosed = false` at `packages/voice-server-websocket/src/transport-host.ts:113`, registers the close listener later at `packages/voice-server-websocket/src/transport-host.ts:159-167`, and gates startup abort on that boolean at `packages/voice-server-websocket/src/transport-host.ts:170-180`. | med | med |
| W1-VE04-1 | Cartesia timestamp offsets accumulate rounded per-chunk milliseconds. | CONFIRMED-REAL | `chunkDurationMs` is computed with `Math.round((audioBytes.length / 2 / this.sampleRate) * 1000)` and added to `contextAudioOffsetMs` at `packages/voice-tts-cartesia/src/index.ts:278-281`; no absolute sample counter is kept. | low | high |
| W1-VE04-2 | Cartesia error frames leak `contextAudioOffsetMs`. | CONFIRMED-REAL | Provider error handling deletes only `activeContexts` at `packages/voice-tts-cartesia/src/index.ts:220-224`; `contextAudioOffsetMs.delete(contextId)` exists on decode errors and done frames at `packages/voice-tts-cartesia/src/index.ts:282-290`, not provider error frames. | med | high |
| W1-VE04-3 | Cartesia can pass `NaN` word timestamps through `typeof === "number"`. | FALSE-POSITIVE | The local guard is only `typeof === "number"` at `packages/voice-tts-cartesia/src/index.ts:253-258`, but provider frames arrive through `JSON.parse` at `packages/voice-tts-cartesia/src/index.ts:202-209`; JSON cannot encode `NaN`, and parse failure clears active offsets via `failActiveContexts()` at `packages/voice-tts-cartesia/src/index.ts:309-312`. | low | high |
| W1-G27-1 | `WebSocketConnection.openSocket()` can hang if `close()` disposes a connecting/reconnecting socket. | CONFIRMED-REAL | `openSocket()` settles only from socket `open/error/close` handlers at `packages/voice-ws/src/index.ts:167-222`. `close()` disposes the socket and nulls it at `packages/voice-ws/src/index.ts:132-140`; the Node adapter `dispose()` removes all listeners at `packages/voice-ws/src/node.ts:47-55`, so the promise can remain unresolved. | high | high |
| W1-G27-2 | Verification timer is not cleared when a socket is disposed during `verify()`. | CONFIRMED-REAL | `verify()` owns a local timeout and `pong` listener at `packages/voice-ws/src/node.ts:24-46`. `dispose()` removes all listeners without clearing that local timer at `packages/voice-ws/src/node.ts:47-65`; the leak is bounded by the verify timeout but still real. | low | high |

## Wave 2

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W2-WT04-1 | `drainAndClose()` can hang until deadline if the socket closes before the queued control frame runs. | CONFIRMED-REAL | `drainAndClose()` has a deadline timer and enqueued control callback but no socket `"close"` listener at `packages/voice-server-websocket/src/outbound-playout-pipeline.ts:125-152`; `playout.close()` clears queued controls via `PacedPlayoutQueue.close()` / `clear()` at `packages/voice-server-websocket/src/paced-playout.ts:59-74`. | high | high |
| W2-WT04-2 | Browser server graceful close can terminate before the 1001 close handshake flushes. | CONFIRMED-REAL | Graceful close awaits `drainAndClose()` then immediately terminates open clients at `packages/voice-server-websocket/src/index.ts:341-348`; `drainAndClose()` settles immediately after `closeWebSocketWithFallback()` at `packages/voice-server-websocket/src/outbound-playout-pipeline.ts:147-150`, not after the peer close. A second terminate loop exists at `packages/voice-server-websocket/src/index.ts:358-364`. | med | high |
| W2-WT03-1 | A 0 ms `PacedPlayoutQueue` control frame still advances the next deadline by 20 ms. | CONFIRMED-REAL | Control frames enqueue with `durationMs: 0` at `packages/voice-server-websocket/src/paced-playout.ts:49-56`, but `pump()` advances by `this.frameDurationMs` whenever more frames remain at `packages/voice-server-websocket/src/paced-playout.ts:107-110`. | med | high |
| W2-WT03-2 | `AudioJitterBuffer.clear(contextId)` does not reset `nextScheduledTime`. | CONFIRMED-REAL | Context-specific clear deletes matching frames and context ID at `packages/voice-client-browser/src/audio.ts:237-252`; only global clear resets `nextScheduledTime` at `packages/voice-client-browser/src/audio.ts:253-267`. New enqueue uses the stale future schedule unless `nextScheduledTime < now` at `packages/voice-client-browser/src/audio.ts:198-203`. | high | high |
| W2-WT03-3 | The jitter-buffer context-clear test does not assert schedule/buffer reset. | CONFIRMED-REAL | `packages/voice-client-browser/src/jitter-buffer.test.ts:118-133` checks only `activeContextIds`; it never checks `bufferedDurationMs`, `nextScheduledTime`, or next-frame schedule. | low | high |
| W2-WT03-4 | Abrupt browser connection drops do not clear the jitter buffer. | CONFIRMED-REAL | `close()` clears jitter buffer at `packages/voice-client-browser/src/index.ts:223-228`; `handleTransportClose()` for unexpected closes only stops keepalive and schedules reconnect at `packages/voice-client-browser/src/index.ts:310-317`. | med | high |

## Wave 3

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W3-WT10-1 | Test cleanup can hang on untracked HTTP keep-alive connections. | CONFIRMED-REAL | `setupTransportTestCleanup()` awaits `httpServer.close()` directly at `packages/voice-server-websocket/src/test-helpers.ts:39-55`; it does not call `closeAllConnections()` or set a fallback timeout. | low | med |
| W3-WT06-1 | `InMemorySessionStore.lease()` has a double-create race. | CONFIRMED-REAL | The map lookup happens before `await create()` and the session is stored after it at `packages/voice-server-websocket/src/session-store.ts:35-48`; concurrent leases for the same missing ID can both create. | high | high |
| W3-WT06-2 | `release(sessionId, 0)` is blocked when a close timer is already active. | CONFIRMED-REAL | `release()` returns early when `managed.closeTimer` exists at `packages/voice-server-websocket/src/session-store.ts:51-55`, before the `retainMs <= 0` eviction path at `packages/voice-server-websocket/src/session-store.ts:55-58`. | high | high |
| W3-WT06-3 | `SessionStore` has no update/save seam for mutable resume state. | CONFIRMED-REAL | Interface exposes `lease/release/get/listAll/clear` only at `packages/voice-server-websocket/src/session-store.ts:24-30`. The browser server mutates leased `ManagedSession` fields in place at `packages/voice-server-websocket/src/index.ts:238-255`, including `currentContextId`, `contextSampleRates`, and `inputSequence`. | high | high |

## Wave 4

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W4-WT07-1 | Client Opus-load failure is not signaled to the server, so Opus downlink remains unusable. | PARTIALLY-REAL | The server still defaults `browserOpusDownlink` to true at `packages/voice-server-websocket/src/index.ts:85-88` and `packages/voice-server-websocket/src/index.ts:149`, and encodes Opus when enabled at `packages/voice-server-websocket/src/index.ts:480-492`. The client falls back locally on Opus-load failure at `packages/voice-client-browser/src/index.ts:489-496` but sends no capability update. The "crash" part is stale: `handleBinaryMessage()` catches decode failures and emits an error at `packages/voice-client-browser/src/index.ts:455-470`; playout still fails. | high | high |
| W4-WT07-2 | Audio sent before the ready/codec negotiation path can go out as PCM before later Opus frames. | CONFIRMED-REAL | `sendAudioPcm()` and `sendFloat32Audio()` call `scheduleUplink()` at `packages/voice-client-browser/src/index.ts:241-245` and `packages/voice-client-browser/src/index.ts:268-280`. `scheduleUplink()` queues only while `codecNegotiation` is non-null at `packages/voice-client-browser/src/index.ts:473-479`; before the first `ready` message, it sends immediately with default `wireCodec = "pcm_s16le"` from `packages/voice-client-browser/src/index.ts:175`. | low | high |
| W4-WT08-1 | `maxConcurrentSessions` is path-scoped only on shared servers. | ALREADY-FIXED | Fixed by `66af756 CR-03 (review): global shared-server admission cap scope (WT-08)`. Current router supports `maxConcurrentSessionsScope` at `packages/voice-server-websocket/src/websocket-upgrade.ts:69-80`, and the cross-path test is `packages/voice-server-websocket/src/admission-control.test.ts:159-198`. | high | high |
| W4-WT08-2 | The shared upgrade router destroys unmatched upgrade paths and hijacks third-party WS endpoints. | CONFIRMED-REAL | Unmatched paths still call `socket.destroy()` in the global upgrade listener at `packages/voice-server-websocket/src/websocket-upgrade.ts:34-44`; current tests expect random scanner path close `1006` at `packages/voice-server-websocket/src/admission-control.test.ts:150-152`. | med | high |

## Wave 5

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W5-WT09-1 | First-audio-played metric is inflated by the 200 ms progress throttle. | CONFIRMED-REAL | `PlayoutProgressEmitter` emits non-complete progress only after `PROGRESS_THROTTLE_MS = 200` at `packages/voice-server-websocket/src/playout-progress.ts:5` and `packages/voice-server-websocket/src/playout-progress.ts:26-34`; `TurnMetricsTracker` records `firstAudioPlayedMs` from the first `tts.playout_progress` with `playedOutMs > 0` at `packages/voice-server-websocket/src/turn-metrics.ts:110-116`. | med | high |
| W5-WT09-2 | Browser turn metrics are lost across session resumption. | CONFIRMED-REAL | `TurnMetricsTracker` owns an in-memory `turns` map at `packages/voice-server-websocket/src/turn-metrics.ts:79-85`; `wireBrowserSessionEvents()` constructs a new tracker per socket at `packages/voice-server-websocket/src/index.ts:537-540`, while `ManagedSession` has no metrics state at `packages/voice-server-websocket/src/session-store.ts:9-17`. | med | high |
| W5-WT09-3 | Browser jitter smoke only impairs uplink, not downlink. | CONFIRMED-REAL | The proxy delays only `clientSocket.on("message")` via `forwardUplink()` at `examples/02-hello-voice-headless/scripts/run-browser-jitter-smoke.ts:242-260`; downlink `upstream.on("message")` forwards directly at `examples/02-hello-voice-headless/scripts/run-browser-jitter-smoke.ts:236-238`. | med | high |
| W5-WT09-4 | Review studio `minPlaybackLeadMs` is initialized to `0`, hiding positive lead. | CONFIRMED-REAL | Initial state sets `minPlaybackLeadMs: 0` at `packages/voice-client-browser/index.html:466-483`; updates use `Math.min(existing ?? leadMs, leadMs)` at `packages/voice-client-browser/index.html:911-915`, so positive leads cannot increase it above zero. | low | high |
| W5-VE01-1 | `TRAILING_INCOMPLETE` is redundantly checked inside the incomplete-prefix branch. | CONFIRMED-REAL | The same `TRAILING_INCOMPLETE.test(normalized)` returns earlier at `packages/voice-turn-pipecat/src/semantic-completeness.ts:67-69` and is rechecked at `packages/voice-turn-pipecat/src/semantic-completeness.ts:77-82`. | low | high |
| W5-VE01-2 | Semantic shortcut delay can be bypassed when the STT final arrives after VAD end. | CONFIRMED-REAL | `handleSpeechEnded()` sets `state.smartTurnComplete = true` before final packets exist at `packages/voice-turn-pipecat/src/index.ts:237-275`; when `handleFinal()` later sees `boundaryAnalyzed && smartTurnComplete && semanticComplete`, it schedules `finalizeDelayMs` at `packages/voice-turn-pipecat/src/index.ts:195-205`, not `semanticShortcutDelayMs`. | high | high |
| W5-VE01-3 | Semantic-defer expiry schedules another incomplete fallback window. | CONFIRMED-REAL | `scheduleSemanticDefer()` waits `semanticDeferFallbackMs` then calls `scheduleIncompleteFallback()` when still incomplete at `packages/voice-turn-pipecat/src/index.ts:334-343`; that adds `incompleteFallbackMs` at `packages/voice-turn-pipecat/src/index.ts:309-319`. Defaults are 4000 ms and 2000 ms at `packages/voice-turn-pipecat/src/index.ts:113-118`. | med | high |

## Wave 6

| ID | Claim | Verdict | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| W6-VE02-1 | Primary-speaker fingerprint is over-fit to a single static 6-band window. | PARTIALLY-REAL | The implementation uses six fixed Goertzel bands and only the first 512 samples at `packages/voice/src/primary-speaker-gate.ts:8-10` and `packages/voice/src/primary-speaker-gate.ts:100-113`. The structure is fragile, but Gemini did not provide an empirical false-reject/false-accept repro against real speech. | med | med |
| W6-VE02-2 | RMS penalty makes the gate volume-sensitive. | PARTIALLY-REAL | Similarity penalizes absolute RMS delta at `packages/voice/src/primary-speaker-gate.ts:145-149`. The volume-sensitivity mechanism is real; the "extremely sensitive" impact is threshold/data dependent and not proven in current tests. | med | med |
| W6-VE02-3 | Positive-vector cosine similarity is biased high, weakening speaker discrimination. | PARTIALLY-REAL | Bands are normalized positive magnitudes at `packages/voice/src/primary-speaker-gate.ts:110-113`, and cosine similarity is used at `packages/voice/src/primary-speaker-gate.ts:131-149`. The math concern is valid; the actual false-accept rate is unmeasured. | med | med |
| W6-VE02-4 | Primary-speaker enrollment includes raw non-speech/noise before the first user turn is locked. | CONFIRMED-REAL | `handleUserAudio()` enrolls every user-audio chunk whenever no TTS is active at `packages/voice/src/voice-agent-session.ts:467-475`; `handleVadAudioForSpeakerGate()` repeats enrollment for `vad.audio` at `packages/voice/src/voice-agent-session.ts:532-537`. This is not gated by `vad.speech_started`. | high | high |
| W6-VE02-5 | Echo dominance check can suppress a primary speaker even when `primarySim > echoSim`. | CONFIRMED-REAL | `shouldCommitBargeIn()` skips a frame when `echoSim >= similarityThreshold` before checking whether primary similarity is higher at `packages/voice/src/primary-speaker-gate.ts:80-88`. | high | high |
| W6-VE02-6 | Speaker-gate suppression can permanently lock out the current VAD turn. | CONFIRMED-REAL | `TurnArbiter.suppress()` resets to idle at `packages/voice/src/turn-arbiter.ts:176-187`; subsequent `vad.speech_activity` has no pending state to evaluate at `packages/voice/src/turn-arbiter.ts:56-61`. In the same VAD turn there may be no second `vad.speech_started` to re-enter pending. | high | high |
| W6-VE02-7 | Assistant profile is never cleared when assistant playout ends. | PARTIALLY-REAL | There is a setter (`observeAssistantPlayout`) at `packages/voice/src/primary-speaker-gate.ts:69-74`, but no clear method. The current session only attempts barge-in when `latestActiveTtsContextId()` is present (`packages/voice/src/voice-agent-session.ts:559-562`), so the "assistant silent" suppression path is not directly reachable; stale state still exists across turns until replaced by new TTS audio. | med | med |
| W6-VE03-1 | Latency filler and main answer are emitted as separate TTS requests, creating possible gap/prosody mismatch. | PARTIALLY-REAL | The filler is sent as immediate `tts.text` at `packages/voice/src/voice-agent-session.ts:607-610`; later LLM deltas are spliced/buffered separately at `packages/voice/src/voice-agent-session.ts:637-658` and flushed at `packages/voice/src/voice-agent-session.ts:668-683`. The two-request structure is real; audible/prosodic severity is provider-dependent and not measured here. | med | med |
| W6-VE03-2 | `stripRedundantFillerPrefix()` removes filler words without a word boundary, mangling words like `Some` and `Wellness`. | CONFIRMED-REAL | Current code checks `lowerBody.startsWith(fillerWord)` and slices by character count at `packages/voice/src/latency-filler.ts:98-108`. Repro: `pnpm exec tsx -e "import { stripRedundantFillerPrefix } from './packages/voice/src/latency-filler.ts'; console.log(stripRedundantFillerPrefix('So,','Some people say...')); console.log(stripRedundantFillerPrefix('Well,','Wellness is important'));"` produced `me people say...` and `ness is important`. | high | high |
| W6-VE03-3 | `LatencyFillerState` can leak on recoverable errors where `llm.done` never arrives. | CONFIRMED-REAL | `LatencyFillerController.states` is cleared only by `clear(contextId)` at `packages/voice/src/latency-filler.ts:28-87`; the session calls `clear()` from `flushTtsText()` at `packages/voice/src/voice-agent-session.ts:698-713`. `handleComponentError()` does not clear filler state on recoverable errors at `packages/voice/src/voice-agent-session.ts:856-886`. | med | high |
