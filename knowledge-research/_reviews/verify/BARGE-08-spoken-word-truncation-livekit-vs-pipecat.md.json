{
  "note": "BARGE-08-spoken-word-truncation-livekit-vs-pipecat.md",
  "overall": "PASS",
  "claims_checked": 8,
  "claims": [
    {
      "claim": "LiveKit synchronizer: target_len = annotated.accumulate_to(elapsed), then forwards pushed_text[forwarded_len:target_len] (annotated speaking-rate path)",
      "kind": "code_ref",
      "cited": "synchronizer.py:344-359",
      "verdict": "SUPPORTED",
      "found": "line 352 'target_len = int(annotated.accumulate_to(elapsed))'; 353 'forwarded_len = len(self._text_data.forwarded_text)'; 355 'd_text = self._text_data.pushed_text[forwarded_len:target_len]'",
      "correction": ""
    },
    {
      "claim": "Fallback to estimated speaking-rate accumulator when no annotated rate",
      "kind": "code_ref",
      "cited": "synchronizer.py:361-365",
      "verdict": "SUPPORTED",
      "found": "361 'elif self._speed_on_speaking_unit:' / 363 'target_speaking_units = self._audio_data.estimated_rate.accumulate_to(elapsed)'",
      "correction": ""
    },
    {
      "claim": "synchronized_transcript returns the elapsed-derived prefix (forwarded_text)",
      "kind": "cross_note_link",
      "cited": "[[BARGE-05-context-reconstruction-vapi]] / synchronizer.py",
      "verdict": "SUPPORTED",
      "found": "line 295-299 'def synchronized_transcript(self) -> str: ... return self._text_data.forwarded_text'; forwarded_text advanced by elapsed at lines 344-380",
      "correction": ""
    },
    {
      "claim": "Pipecat output transport routes any pts-bearing frame to handle_timed_frame",
      "kind": "code_ref",
      "cited": "base_output.py:379-380",
      "verdict": "SUPPORTED",
      "found": "379 'elif frame.pts:' / 380 'await sender.handle_timed_frame(frame)'",
      "correction": ""
    },
    {
      "claim": "handle_timed_frame queues frame on a clock task keyed by (pts, ...)",
      "kind": "code_ref",
      "cited": "base_output.py:616",
      "verdict": "SUPPORTED",
      "found": "616 'await self._clock_queue.put((frame.pts, next(self._clock_queue_counter), frame))'",
      "correction": ""
    },
    {
      "claim": "Word-level TTSTextFrames reach assistant aggregator; on interruption aggregation contains exactly spoken words",
      "kind": "code_ref",
      "cited": "llm_response_universal.py:1880",
      "verdict": "SUPPORTED",
      "found": "1880 'async def _handle_text(self, frame: TextFrame):' ... 1897 'self._aggregation.append(' — TextFrames (incl. TTSTextFrame) appended to assistant aggregation",
      "correction": ""
    },
    {
      "claim": "Vapi mechanism = trust TTS-provider word timestamps to reconstruct which words the user actually heard",
      "kind": "source_figure",
      "cited": "vapi-pipeline-2 line 56",
      "verdict": "SUPPORTED",
      "found": "line 56 'Use word-level timestamps from the TTS provider to reconstruct exactly which words the user actually heard before they interrupted'. Note: actual file is blogs/vapi-pipeline-part-2.md (shortname maps to vapi-pipeline-2); line 56 matches exactly.",
      "correction": ""
    },
    {
      "claim": "Pipecat clock_queue tuple keyed by pts then counter (note says '(pts, ...)')",
      "kind": "mechanism",
      "cited": "base_output.py:616",
      "verdict": "SUPPORTED",
      "found": "tuple is (frame.pts, next(self._clock_queue_counter), frame) — pts is primary sort key, matches note's '(pts, ...)'",
      "correction": ""
    }
  ],
  "summary": "All 8 checkable assertions verified against ground truth. Both frontmatter code_refs (synchronizer.py:344, base_output.py:379) land exactly. Inline line ranges for LiveKit annotated/estimated paths, Pipecat handle_timed_frame/clock-queue, and aggregator _handle_text all match. Vapi quote at line 56 is verbatim. Minor: cited source shortname 'vapi-pipeline-2' maps to a file actually named vapi-pipeline-part-2.md, but per the review source map this is the registered alias and content is exact; not a defect of the note."
}
