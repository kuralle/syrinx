{
  "note": "STT-07-provider-fallback.md",
  "overall": "PARTIAL",
  "claims_checked": 9,
  "claims": [
    {
      "claim": "FallbackAdapter _recognize_impl tries each available STT in order; on exception marks unavailable, emits stt_availability_changed, moves to next",
      "kind": "code_ref",
      "cited": "agents/livekit-agents/livekit/agents/stt/fallback_adapter.py:211-243",
      "verdict": "SUPPORTED",
      "found": "async def _recognize_impl (line 211); for i, stt in enumerate(self._stt_instances) ... except Exception: if stt_status.available: stt_status.available = False; self.emit('stt_availability_changed', AvailabilityChangedEvent(stt=stt, available=False))",
      "correction": ""
    },
    {
      "claim": "_try_recovery spawns a background task that re-probes the failed provider and flips available=True on success",
      "kind": "code_ref",
      "cited": "agents/livekit-agents/livekit/agents/stt/fallback_adapter.py:175-209",
      "verdict": "SUPPORTED",
      "found": "def _try_recovery (line 175) ... async def _recover_stt_task ... stt_status.available = True; logger.info(f'{stt.label} recovered'); self.emit('stt_availability_changed', ...); recovering_recognize_task = asyncio.create_task(_recover_stt_task(stt))",
      "correction": ""
    },
    {
      "claim": "If all fail it still retries them all",
      "kind": "code_ref",
      "cited": "agents/livekit-agents/livekit/agents/stt/fallback_adapter.py:220-226",
      "verdict": "SUPPORTED",
      "found": "all_failed = all(not stt_status.available for stt_status in self._status); if all_failed: logger.error('all STTs are unavailable, retrying..'); ... if stt_status.available or all_failed:",
      "correction": ""
    },
    {
      "claim": "_connection_handler wraps the socket in a while True retry loop that reconnects automatically after transient errors",
      "kind": "code_ref",
      "cited": "pipecat/src/pipecat/services/deepgram/stt.py:622-650",
      "verdict": "SUPPORTED",
      "found": "async def _connection_handler (622): 'Reconnects automatically after transient errors.' ; while True: ... except Exception as e: logger.warning(f'{self}: Connection lost, will retry: {e}')",
      "correction": ""
    },
    {
      "claim": "_keepalive_handler sends a KeepAlive every 5s because Deepgram closes inactive connections after 10 seconds (NET-0001)",
      "kind": "code_ref",
      "cited": "pipecat/src/pipecat/services/deepgram/stt.py:652-665",
      "verdict": "SUPPORTED",
      "found": "async def _keepalive_handler (652): 'Deepgram closes inactive connections after 10 seconds (NET-0001 error). Sending every 5 seconds...' ; while True: await asyncio.sleep(5) ... send_keep_alive(ListenV1KeepAlive(...))",
      "correction": ""
    },
    {
      "claim": "On reconnect Pipecat replays buffered audio only after _connection_ready is set",
      "kind": "code_ref",
      "cited": "pipecat/src/pipecat/services/deepgram/stt.py:463-479",
      "verdict": "SUPPORTED",
      "found": "_do_reconnect (463): await self._disconnect(); await self._connect(); await asyncio.wait_for(self._connection_ready.wait(), timeout=5.0). _reconnect() then replays buffer at stt_service.py:628-631 after _do_reconnect returns.",
      "correction": ""
    },
    {
      "claim": "Buffered audio appended while reconnecting",
      "kind": "code_ref",
      "cited": "pipecat/src/pipecat/services/stt_service.py:361-363",
      "verdict": "SUPPORTED",
      "found": "if self._reconnecting: (361) self._reconnect_audio_buffer.append((frame, direction)) (362) return (363)",
      "correction": ""
    },
    {
      "claim": "Rapida enables keepalive at the SDK client (EnableKeepAlive: true) at deepgram/stt.py:80",
      "kind": "code_ref",
      "cited": "pipecat/src/pipecat/services/deepgram/stt.py:80 (note writes 'deepgram/stt.py:80')",
      "verdict": "MISCITED",
      "found": "pipecat deepgram/stt.py:80 is 'keywords: Any | None = None' (an __init__ param), NOT EnableKeepAlive. The real 'EnableKeepAlive: true' lives in the Rapida/voice-ai Go file at voice-ai/api/assistant-api/internal/transformer/deepgram/stt.go:80",
      "correction": "voice-ai/api/assistant-api/internal/transformer/deepgram/stt.go:80 (Go file, not pipecat .py). Mechanism is true; path/extension/repo wrong."
    },
    {
      "claim": "Vapi: Multiple STT providers with automatic fallback if primary fails; handle provider-specific quirks while keeping consistent behavior",
      "kind": "source_figure",
      "cited": "vapi-pipeline-2:33",
      "verdict": "SUPPORTED",
      "found": "vapi-pipeline-part-2.md:33 'Multiple STT providers with **automatic fallback** if primary fails; handle provider-specific quirks while keeping consistent behavior.' (source file is named vapi-pipeline-part-2.md, shortname maps correctly)",
      "correction": ""
    },
    {
      "claim": "Deepgram guidance: Streaming connections should implement reconnection logic with exponential backoff",
      "kind": "source_figure",
      "cited": "deepgram-ebook:592",
      "verdict": "SUPPORTED",
      "found": "deepgram-voice-agent.parsed.md:592 'Streaming connections should implement reconnection logic with exponential [backoff]'",
      "correction": ""
    }
  ],
  "summary": "9 of 10 checkable assertions SUPPORTED at or within the cited window. Both source quotes (vapi-pipeline-part-2:33, deepgram-ebook:592) are verbatim. The single defect: the Rapida 'EnableKeepAlive: true' claim is cited as 'deepgram/stt.py:80' which, per the note's own source map, resolves to pipecat's deepgram/stt.py (line 80 = 'keywords:' param). The real code is in the voice-ai/Rapida Go file stt.go:80. Mechanism true, path miscited. No WRONG claims."
}
